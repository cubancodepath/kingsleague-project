---
import Logo from './Logo.astro'

const response = await fetch('https://api.kingsleague.bjvalmaseda.com/teams')
const teams = await response.json()
console.log(teams[0])
---

<header
  class='flex items-center w-screen bg-gradient-to-b from-[#f66900] via-[#ff6900] to-[#e66000] p-8 gap-x-8'
>
  <Logo />
  <div class='flex flex-col h-full gap-4'>
    <nav>
      <ul class='flex gap-x-4 [&>li>a:hover]:scale-150'>
        {
          teams.map(({ id, image, name }) => (
            <li>
              <a class='group block transition' href={`/team/${id}`}>
                <img
                  class='group-hover:drop-shadow-[0px_0px_8px_#fff] aspect-square w-12 drop drop-shadow-[0px_0px_4px_#fff]'
                  src={image}
                  alt={`Escudo del equipo ${name}`}
                  title={name}
                />
              </a>
            </li>
          ))
        }
      </ul>
    </nav>
    <nav>
      <ul
        class='flex gap-x-4 text-white font-semibold [&>li>a]:opacity-80 text-sm [&>li>a:hover]:underline [&>li>a:hover]:opacity-100'
      >
        <li>
          <a href='/equipos'>Equipos</a>
        </li>
        <li>
          <a href='/calendario'>Calendario</a>
        </li>
        <li>
          <a href='/estadisticas'>Estadística</a>
        </li>
        <li>
          <a href='/calendario'>Contacto</a>
        </li>
        <li>
          <a href='/reglamento'>Reglamento</a>
        </li>
      </ul>
    </nav>
  </div>
</header>
